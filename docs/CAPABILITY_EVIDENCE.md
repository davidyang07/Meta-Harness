# Capability evidence

<!-- generated-at: 2026-09-02T23:22:55.486183+00:00 -->
<!-- commit: 58938f94a9baad727458e7415be8c870899516f3 -->

Generated by `uv run meta-harness report capability-evidence`. Every
row below is derived from committed artifacts — raw trial rows,
compiled graphs, declared dependencies, verification reports — at the
moment the command ran. Nothing here is hand-entered, and a claim with
no supporting artifact reports `UNSUPPORTED` rather than being
softened. No row asserts that a measurement cleared a target.

CI regenerates this file and fails if it disagrees with the raw
results, so a stale or edited number cannot survive a push.

## Headline

> No measured result: the canonical experiment has not been run.

**11 PASS · 0 FAIL · 5 UNSUPPORTED**

## Claims

| Claim | Status | Value | Basis |
|---|---|---|---|
| Trace-driven agent optimizer | **PASS** | `{"trace_artifacts_written":["orient.json","plan.json","act-tools.jsonl","act-messages.jsonl","verify.json","score.json"],"proposer_inputs":["evolution_summary","frontier_val","traces"]}` | every inner-loop trial writes orient/plan/act/verify/score traces, and the proposer's prior is assembled from the branch's own traces, evolution log and Pareto frontier — so each proposal is conditioned on recorded execution, not on a scalar score |
| Separate execution and self-improvement LangGraph state machines | **PASS** | `{"inner":["act","orient","plan","submit","verify"],"outer":["benchmark","propose","update_frontier","validate"]}` | both graphs compiled and their node sets read back; the sets are disjoint, so these are two machines and not one |
| Canonical 200 task trials executed | **UNSUPPORTED** | — | no published pass-rate experiment under benchmarks/results/. Run `uv run meta-harness canonical-experiment` with credentials, then commit the result directory. |
| Baseline pass rate | **UNSUPPORTED** | — | no published pass-rate experiment under benchmarks/results/. Run `uv run meta-harness canonical-experiment` with credentials, then commit the result directory. |
| Evolved pass rate | **UNSUPPORTED** | — | no published pass-rate experiment under benchmarks/results/. Run `uv run meta-harness canonical-experiment` with credentials, then commit the result directory. |
| Absolute percentage-point improvement | **UNSUPPORTED** | — | no published pass-rate experiment under benchmarks/results/. Run `uv run meta-harness canonical-experiment` with credentials, then commit the result directory. |
| Generalisation on unseen holdout tasks | **UNSUPPORTED** | — | no published holdout experiment. Generalisation to unseen tasks is a separate claim from the search-set number, not a substitute for it. |
| Git-style run versioning in PostgreSQL | **PASS** | `{"checkpoints":36,"branches":2,"immutable":true}` | checkpoint DAG with immutable ids and parent references, branch refs persisted in branches.json, per-branch working trees under runs/<run>/threads/<thread>/; every stored checkpoint re-read and confirmed to still hash to what it hashed to |
| Exact replay of a recorded execution from any checkpoint | **PASS** | `{"replays":2,"from_checkpoint":1,"all_verified":true,"model_calls_issued":0,"recorded_models":["scripted-offline"]}` | each recorded trial re-executed against its tape, from the start of the run and from a mid-run checkpoint; node sequence, per-step state hashes and final state hash all matched, the tape was consumed exactly, and no model call was issued. Recorded model(s): scripted-offline. |
| Branching from any persisted checkpoint | **PASS** | 2 | each branch is recorded with the checkpoint it forked from and its own working tree under runs/<run>/threads/<thread>/; the registry reloads from branches.json after a restart |
| Stack: LangGraph | **PASS** | yes | declared and used |
| Stack: Weights & Biases | **PASS** | `{"mode":"offline","logged":3}` | optional integration: the repository, its tests and every CLI command run with no W&B account and no network |
| Stack: FastAPI | **PASS** | yes | declared and used |
| Stack: Docker | **PASS** | yes | declared and used |
| Stack: PostgreSQL | **PASS** | yes | declared and used |
| Stack: Pydantic | **PASS** | yes | declared and used |

## Evidence paths

- **Trace-driven agent optimizer** — `backend/app/meta_harness/inner.py`, `backend/app/meta_harness/proposer.py`, `backend/app/meta_harness/runs.py`
- **Separate execution and self-improvement LangGraph state machines** — `backend/app/meta_harness/inner.py`, `backend/app/meta_harness/outer.py`
- **Git-style run versioning in PostgreSQL** — `docs/evidence/version-graph.json`
- **Exact replay of a recorded execution from any checkpoint** — `docs/evidence/replay-verification.json`
- **Branching from any persisted checkpoint** — `docs/evidence/version-graph.json`
- **Stack: LangGraph** — `declared dependency `langgraph>=0.2``, `used in 8 module(s): backend/app/api/runs.py, backend/app/cli.py, backend/app/meta_harness/branches.py`
- **Stack: Weights & Biases** — `declared dependency `wandb>=0.18``, `used in 1 module(s): backend/app/meta_harness/tracking.py`, `docs/evidence/wandb-offline.json`
- **Stack: FastAPI** — `declared dependency `fastapi>=0.115``, `used in 7 module(s): backend/app/api/branches.py, backend/app/api/checkpoints.py, backend/app/api/events.py`
- **Stack: Docker** — ``infra/docker-compose.yml``, ``infra/Dockerfile``, ``infra/healthcheck.py``, ``.dockerignore``, ``scripts/docker_smoke.sh``
- **Stack: PostgreSQL** — `declared dependency `langgraph-checkpoint-postgres>=2.0``, `used in 1 module(s): backend/app/meta_harness/persistence.py`
- **Stack: Pydantic** — `declared dependency `pydantic>=2.9``, `used in 3 module(s): backend/app/api/forks.py, backend/app/api/memory.py, backend/app/api/runs.py`

## How to reproduce every row

```bash
# the measured rows: recompute the published summary from raw trials
cd backend && uv run pytest tests/test_capability_evidence.py -q

# the structural rows: compile both graphs and read their node sets
uv run meta-harness report capability-evidence --json

# the replay row: re-execute a recorded trial against its tape
uv run meta-harness verify-replay <recordings-dir>

# the version-graph row: read the checkpoint DAG out of Postgres
uv run meta-harness report version-graph <run-name>
```

## What `UNSUPPORTED` means here

The measurement machinery exists and is tested, but the artifact that
would settle the claim has not been produced in this repository. For
the pass-rate rows that means the canonical experiment has not been
run against a real model — it costs money and needs credentials. The
one command that produces every missing artifact is
`uv run meta-harness canonical-experiment`.
