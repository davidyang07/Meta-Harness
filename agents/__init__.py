"""Top-level ``agents/`` package.

Holds the immutable baseline harness (``baseline.py``) and any
hand-tuned candidates committed to the repo. Proposer-written candidates
land per-run under ``runs/{run_id}/agents/`` and are imported dynamically.
"""
